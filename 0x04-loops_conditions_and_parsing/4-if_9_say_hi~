#!/usr/bin/env bash
# Bash script that displays Best School 10 times.
count=1
while [ $count -le 10 ];
do
    if [ $count == 9 ]; then
	echo "Best School"
	echo "Hi"
    else
	 echo "Best School"
    count=$((count + 1))
done
